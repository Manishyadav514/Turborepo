{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf dist && yarn build && nodemon ./dist/src/index.js && next dev --port 3003",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.6.3",
    "eslint": "^8.21.0",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  }
}
